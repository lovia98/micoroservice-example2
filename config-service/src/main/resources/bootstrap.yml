server:
  port: 8888
spring:
  profiles:
    active: native
  application:
    name: cloud-config
  cloud:
    config:
      server:
        native:
          search-locations: file://${user.home}/devj/micoroservice-example/config
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8061/eureka/
