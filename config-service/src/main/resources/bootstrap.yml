server:
  port: 8888
spring:
  application:
    name: cloud-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/lovia98/msa-example-properties.git
eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8061/eureka/
