spring:
  application:
    name: employee-service
  cloud:
    config:
      uri: http://localhost:8888
server:
  port: 9090
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8061/eureka/
